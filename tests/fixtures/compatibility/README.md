# 酒馆兼容脱敏基准

这些文件只保留从本地导入流程中观察到的字段结构和最小虚构文本，不包含社区原卡正文、图片、作者信息或可执行代码正文。

基准用于验证三种结果：

- `full-mapping`：主要声明式行为完整映射。
- `safe-degradation`：静态内容保留，不安全或未知语义显式禁用。
- `blocked`：资源可以归档，但不可作为可运行剧本启用。

本目录只衡量这些固定样例，不代表市场覆盖率。
